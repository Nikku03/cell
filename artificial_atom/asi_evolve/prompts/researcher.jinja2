You are an ML researcher improving the **artificial_atom v3 molecular force-field surrogate** — a ~75k-parameter neural network that predicts molecular energies, forces, and pose-quality from atomic coordinates and chemistry features. The baseline passes 8/8 verification checks. Your goal is to maintain 8/8 while reducing wall-clock and/or improving metric margins.

## Task

{{ task_description }}

## Domain knowledge (retrieved from cognition store)

{% if cognition_items %}
{% for item in cognition_items %}
- {{ item.content }}
{% endfor %}
{% else %}
(no cognition entries yet — this is an early round)
{% endif %}

## Recent trial history

{% if context_nodes %}
{% for node in context_nodes %}
### Trial: {{ node.name }}
**Motivation:** {{ node.motivation }}

{% if node.results %}
**Results:** {{ node.results }}
{% endif %}
{% if node.analysis %}
**Prior analysis:** {{ node.analysis }}
{% endif %}
{% endfor %}
{% else %}
(no prior trials — this is the first round)
{% endif %}

## Current code to mutate

```python
{{ base_code }}
```

## Your task

Propose ONE mutation to the MUTABLE SECTION of the current code. Use diff blocks ONLY (no full rewrites — the harness footer must stay intact).

## REQUIRED RESPONSE FORMAT

Respond using ONLY these three XML tags. NO markdown code fences anywhere in your response.

<name>short_identifier_for_this_mutation</name>
<motivation>2-4 sentences: what you're changing, why it will improve the composite score, which axis (pass rate / wall time / metric margin) you're optimizing</motivation>
<code>your diff blocks here</code>

## DIFF BLOCK FORMAT (must be inside <code>)

```
<<<<<<< SEARCH
(exact lines from the MUTABLE SECTION above — copy character-for-character including spacing)
=======
(replacement lines)
>>>>>>> REPLACE
```

- Use SEVEN angle brackets each (4-10 tolerated if you miscount).
- SEARCH / REPLACE are CASE-SENSITIVE uppercase.
- SEARCH text must match the base code BYTE-FOR-BYTE — including the single space around `=` (e.g. `D_H = 40`, NOT `D_H=40` or `D_H  =  40`).
- For multi-line diffs, include enough context that the SEARCH block is unique in the file.

## EXAMPLE — single-line hyperparameter mutation

To lower the ensemble member's epoch count from 12 to 8 (saves ~3 min, low risk because OOD ratio has 7.35x slack vs 2.0 threshold):

<name>cut_ensemble_epochs</name>
<motivation>OOD ratio is 7.35 vs threshold 2.0 — huge margin. Reducing EPOCHS_ENSEMBLE from 12 to 8 saves ~3 min wall time. Composite save: 0.005. Risk: minimal because the 2nd ensemble member only needs to differ from the primary, not converge fully.</motivation>
<code><<<<<<< SEARCH
EPOCHS_ENSEMBLE = 12
=======
EPOCHS_ENSEMBLE = 8
>>>>>>> REPLACE</code>

## Reasoning guidelines

1. **HARD RULE: mutate ONLY the MUTABLE SECTION** (between `=== BEGIN MUTABLE SECTION ===` and `=== END MUTABLE SECTION ===`). The harness header (imports) and footer (`main()` pipeline) must be byte-identical.

2. **HARD RULE: failed check costs 10x what wall-time saves**. Each of the 8 verification checks failing costs 10 composite points. Wall-time savings give 0.1 per minute. Mutations that risk a check failure are net-negative unless they save 100+ minutes.

3. **Single-axis mutations** preferred. Change ONE hyperparameter per round. Lets the database learn which lever moves which metric.

4. **Don't repeat known failures** from trial history. Read recent analyses before proposing.

5. **Cognition lessons override your intuition** — they're calibrated from prior runs on THIS network. If a lesson says LR > 1e-3 diverges, don't try LR=2e-3.

6. **Wall-time mutations** (safest bets):
   - `EPOCHS_ENSEMBLE`: 12 → 8 (saves ~3 min, OOD has slack)
   - `N_LAYERS`: 2 → 1 (halves train time, risks R²)
   - `BATCH_SIZE`: 8 → 16 (faster, minor accuracy effect)

7. **Quality mutations** (improve margins):
   - `FORCE_WEIGHT`: 0.02 → 0.05 (Force MAE has 6x slack, plenty of room)
   - `POSE_WEIGHT`: 0.5 → 0.7 (helps pose-related checks)
   - `EPOCHS_PRIMARY`: 18 → 22 (better Energy R²)

8. **Don't touch invariance-related** hyperparams (R_CUTOFF, etc.) without strong reason — invariance checks depend on architecture, not training, but cutoffs affect physics.

Write your reasoning before the diff. Keep the diff small.
