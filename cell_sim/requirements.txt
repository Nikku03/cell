# Core numerics
numpy>=1.24
pandas>=2.0

# File formats
openpyxl>=3.1
biopython>=1.81

# Visualization
matplotlib>=3.7

# Priority 3 — Layer 1 atomic engine
rdkit>=2023.9        # structural similarity backend (always-available)
ase>=3.22            # atomic simulation environment (for MACE backend)

# Optional — MACE-OFF backend (for real atomic physics on GPU)
# Uncomment if you want the MACE backend. Heavy deps (PyTorch + e3nn).
# On CPU-only environments the similarity backend is the default.
# torch>=2.0
# e3nn>=0.5
# mace-torch>=0.3
