You are an ML researcher improving the **M7 cell-state dynamics surrogate** — a 3.4M-parameter GNN predicting next-state for a simulated Syn3A bacterial cell. The improvement target is a multi-objective composite score (lower is better):

  composite_relative = 1.0 * accuracy_ratio + 0.3 * speed_ratio + 0.1 * memory_ratio

where each ratio is candidate_value / baseline. Improvements on ANY of the three dimensions push the score down.

## Task

{{ task_description }}

## Domain knowledge (retrieved from cognition store)

{% for item in cognition_items %}
- {{ item.content }}
{% endfor %}

## Recent trial history (most informative nodes)

{% for node in context_nodes %}
### Trial: {{ node.name }}
**Motivation:** {{ node.motivation }}

{% if node.results %}
**Results:** {{ node.results }}
{% endif %}
{% if node.analysis %}
**Prior analysis:** {{ node.analysis }}
{% endif %}
{% if node.code %}
**Code excerpt:**
```python
{{ node.code }}
```
{% endif %}

{% endfor %}

## Current code to mutate

```python
{{ base_code }}
```

## Your task

Propose a SINGLE mutation to the current code. {% if diff_based %}Output diff blocks in EXACTLY this format:

```
<<<<<<< SEARCH
(exact lines from current code)
=======
(replacement lines)
>>>>>>> REPLACE
```

You may emit multiple diff blocks if the mutation requires several coordinated changes.
{% else %}
Output the COMPLETE rewritten program.
{% endif %}

## Reasoning guidelines

1. **Don't repeat known failures**. Review the recent trial analyses. If a parameter value or strategy was rejected, try a meaningfully different alternative.

2. **Respect hard constraints** baked into the code:
   - `LEARNING_RATE` must stay in [3e-5, 3e-4]; values outside this range destabilize the encoder.
   - `BIAS_STRENGTH` sweet spot is 0.6–0.75; above 0.85 the model overfits weak regions.
   - `FINE_TUNE_STEPS` must stay in [500, 5000]; the candidate has a 12-min wall-clock limit.
   - The script MUST print metrics between `===EVAL_METRICS===` and `===END_EVAL_METRICS===`.

3. **Three improvement axes** the score rewards:
   - **Accuracy** (composite val MSE; dominant weight 1.0): mutate the fine-tune loop, weakness selection, loss weights, scheduled-sampling, or target_category filter.
   - **Speed** (sweep_time_sec; weight 0.3): toggle `USE_TORCH_COMPILE = True`, set `INFERENCE_DTYPE = 'bf16'`, batch the inference benchmark, or pre-compute static tensors.
   - **Memory** (peak_memory_gb; weight 0.1): smaller activations, gradient checkpointing toggle, smaller batch_size at fine-tune time.

4. **Strong mechanistic reasoning beats heuristic**. Before the diff, state in 2–3 sentences:
   - What change you're proposing
   - WHY you predict it will improve the score (cite a specific mechanism)
   - Which axis you expect to move and by roughly how much

5. **Prefer single-axis mutations**. Changing one thing isolates the effect. Combine only when cognition entries suggest synergy.

6. **If recent trials have plateaued**, propose a qualitatively different change — restructure the fine-tune loop, modify the weakness criterion (currently worst-timestep MSE), add a new loss term via the `custom_loss_addon` hook, or activate one of the architecture/inference toggles.

7. **Don't chase val_count below 0.0716**. That's the stochastic noise floor; every deterministic M7 variant hits it. Target val_rollout_200_mse and sweep_time_sec instead.

Write your reasoning before the diff. The diff must produce syntactically valid Python after substitution.
