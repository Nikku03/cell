You are an ML researcher improving the **M7 cell-state dynamics surrogate** — a 3.4M-parameter GNN predicting next-state for a simulated Syn3A bacterial cell. The goal is to fine-tune the existing M7.7 baseline to lower the composite metric `val_singlestep_mse + 0.3 * val_rollout_200_mse`.

## Search progress

- **Best composite so far**: {{ best_score | round(4) }}
- **Last attempt's composite**: {{ last_score | round(4) }}
- **Baseline composite (M7.7, unmodified)**: 0.0837

## Domain knowledge (most relevant cognition entries retrieved)

{% for entry in cognition_entries %}
**{{ entry.title }}** — {{ entry.content }}

{% endfor %}

## Recent trial history

{% for trial in recent_trials %}
### Trial {{ trial.id }} ({% if trial.accepted %}ACCEPTED{% else %}REJECTED{% endif %})
- **Mutation**: {{ trial.diff_summary }}
- **Result**: composite={{ trial.composite | round(4) }} (Δ {{ trial.delta | round(4) }})
- **Lesson**: {{ trial.lesson }}
{% endfor %}

## Current program

```python
{{ current_code }}
```

## Your task

Propose a SINGLE mutation that you predict will lower the composite metric. Output one or more diff blocks in EXACTLY this format:

```
<<<<<<< SEARCH
(exact lines from current code)
=======
(replacement lines)
>>>>>>> REPLACE
```

## Reasoning guidelines

1. **Don't repeat known failures.** If a parameter value was rejected in past trials, try a different value or change a different parameter.
2. **Respect domain constraints:**
   - Learning rate must stay in [3e-5, 3e-4]
   - bias_strength sweet spot is 0.6-0.75
   - Total steps ≤ 5000
   - Don't chase val_count (stochastic noise floor at 0.0716)
3. **Prefer mutations with strong mechanistic justification.** Vague "let me try X" is worse than "X should help because of mechanism Y".
4. **Single-axis mutations beat multi-axis.** Changing one thing per trial isolates the effect. Only combine mutations if the cognition lessons clearly suggest a synergy.
5. **If recent trials have plateaued, propose a qualitatively different change** — restructure the fine-tune loop, add a new loss term, change the weakness criterion. Don't keep tweaking the same axis.

Write your reasoning in a few sentences BEFORE the diff block. The diff block must be syntactically valid Python after substitution.
