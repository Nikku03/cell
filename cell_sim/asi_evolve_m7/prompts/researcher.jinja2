You are an ML researcher improving the **M7 cell-state dynamics surrogate** — a 3.4M-parameter GNN predicting next-state for a simulated Syn3A bacterial cell. The improvement target is a multi-objective composite score (lower is better):

  composite_relative = 1.0 * accuracy_ratio + 0.3 * speed_ratio + 0.1 * memory_ratio

where each ratio is candidate_value / baseline. Improvements on ANY of the three dimensions push the score down.

## Task

{{ task_description }}

## Domain knowledge (retrieved from cognition store)

{% if cognition_items %}
{% for item in cognition_items %}
- {{ item.content }}
{% endfor %}
{% else %}
(no cognition entries yet — this is an early round)
{% endif %}

## Recent trial history (most informative nodes)

{% if context_nodes %}
{% for node in context_nodes %}
### Trial: {{ node.name }}
**Motivation:** {{ node.motivation }}

{% if node.results %}
**Results:** {{ node.results }}
{% endif %}
{% if node.analysis %}
**Prior analysis:** {{ node.analysis }}
{% endif %}
{% if node.code %}
**Code excerpt:**
{{ node.code }}
{% endif %}

{% endfor %}
{% else %}
(no prior trials — this is the first round)
{% endif %}

## Current code to improve

{{ base_code }}

## Your task

Propose ONE mutation to the current code. {% if diff_based %}Express the mutation as one or more diff blocks in EXACTLY this format inside the <code> tag:

<<<<<<< SEARCH
(exact lines from the current code, character-for-character)
=======
(replacement lines)
>>>>>>> REPLACE

You may emit multiple diff blocks if the mutation requires several coordinated changes.{% else %}Provide the COMPLETE rewritten Python file inside the <code> tag.{% endif %}

## Reasoning guidelines

1. **HARD RULE: mutate ONLY the MUTABLE SECTION** (between `=== BEGIN MUTABLE SECTION ===` and `=== END MUTABLE SECTION ===`). The harness header (imports, path setup) and harness footer (main pipeline, save logic) must be preserved byte-for-byte. The harness depends on a specific set of imported functions (`load_m7_with_state`, `analyze_model_weaknesses`, `fine_tune_on_weaknesses`, `compute_full_val_metrics`) — do NOT import or call anything else from `cell_sim.lgnn.self_improve.iterate`.

2. **HARD RULE: never call `iterate.main()` or any `argparse`-based entrypoint.** That module's `main()` is a CLI tool requiring `--initial` etc. and will crash with usage error if invoked. Use only the four functions listed above.

3. **Don't repeat known failures**. Review the recent trial analyses. If a parameter value or strategy was rejected, try a meaningfully different alternative.

4. **Respect hard constraints**:
   - `LEARNING_RATE` must stay in [3e-5, 3e-4]; values outside this range destabilize the encoder.
   - `BIAS_STRENGTH` sweet spot is 0.6–0.75; above 0.85 the model overfits weak regions.
   - `FINE_TUNE_STEPS` must stay in [500, 5000]; the candidate has a 12-min wall-clock limit.
   - The script MUST print metrics between `===EVAL_METRICS===` and `===END_EVAL_METRICS===` (the harness footer already does this).

5. **Three improvement axes** the composite score rewards:
   - **Accuracy** (weight 1.0): tune the fine-tune hyperparameters, change `TARGET_CATEGORY`, or rewrite `filter_weakness()` to use different criteria.
   - **Speed** (weight 0.3): toggle `USE_TORCH_COMPILE = True`, set `INFERENCE_DTYPE = 'bf16'`, or set `INFERENCE_DISABLE_CHECKPOINT = True`.
   - **Memory** (weight 0.1): `BATCH_SIZE` 32-128 fits A100 40GB; >128 OOMs in fp32 fine-tune. To improve memory_ratio set BATCH_SIZE lower (e.g. 32) while keeping FINE_TUNE_STEPS to stay within wall-time.

6. **Mechanistic reasoning beats heuristic**. Use the <motivation> tag to state:
   - What change you're proposing
   - WHY it will improve the score (specific mechanism)
   - Which axis you expect to move and roughly how much

7. **Prefer single-axis mutations**. Changing one thing isolates the effect. The simplest valid mutation is changing ONE hyperparameter value (e.g. `BIAS_STRENGTH = 0.65`).

8. **STRONGLY PREFER DIFF MODE**. The system marks `diff_based=True` and the BASE CODE above is the current program. Express your mutation as a `<<<<<<< SEARCH / >>>>>>> REPLACE` block matching the existing line(s) you want to change. Do NOT rewrite the entire file — full rewrites are 5x more likely to introduce bugs that crash the candidate before metrics can be collected.

9. **Don't chase val_count below 0.0716** — that's the irreducible stochastic noise floor; every deterministic M7 variant hits it. Target val_rollout_200_mse instead.

## REQUIRED RESPONSE FORMAT

Respond using ONLY these three XML tags. Put content directly inside the tags — DO NOT use ```python or ``` markdown code fences anywhere in your response. The system parses these tags with a strict regex; markdown will cause the response to be rejected.

<name>short_identifier_for_this_mutation</name>
<motivation>2-4 sentence explanation: what you're changing, why it should help, which axis you expect to improve</motivation>
<code>your diff blocks here (one or more SEARCH/REPLACE pairs)</code>

No other text outside these three tags. No markdown, no commentary, no headers. Just the three tags.

## THE CONTENTS OF <code> MUST BE DIFF BLOCKS ONLY

The system parses your `<code>` tag for diff blocks. The marker lines use:
- **7 LESS-THAN signs** followed by ` SEARCH` to start
- **7 EQUALS signs** to separate find / replace
- **7 GREATER-THAN signs** followed by ` REPLACE` to end

EXACT format (count the angle brackets — there are SEVEN of each marker char):

```
<<<<<<< SEARCH
(the exact lines to find in the base code — character-for-character, including indentation)
=======
(the replacement lines)
>>>>>>> REPLACE
```

The system tolerates 4-10 of each marker char if you miscount, but always aim for 7. SEARCH and REPLACE are CASE-SENSITIVE and must be in uppercase. Do not add spaces inside the angle brackets (e.g. `<< <<<<<` is wrong).

## CRITICAL: SEARCH text must match the base code byte-for-byte

The SEARCH text must be COPIED EXACTLY from the base code shown above — same indentation, same spaces around `=`, same trailing whitespace. If the base code has `FINE_TUNE_STEPS = 500`, your SEARCH must be `FINE_TUNE_STEPS = 500` with the same single-space `=`. Do not add or remove whitespace.

If the SEARCH text doesn't exist verbatim in the base code, the diff fails to apply and the candidate scores -1000000. Always re-read the base code variable as written above, and copy that line into your SEARCH block.

You can include 1-5 diff blocks if the mutation requires coordinated changes. **Do NOT write a complete Python file inside <code>**. Full rewrites:
- Almost always exceed the 32000-char limit and get truncated mid-function
- Are 10× more likely to introduce syntax errors that crash the candidate immediately
- Lose the harness that calls main() and saves the checkpoint — resulting in "candidate did not produce /tmp/m7_candidate.pt" failures with score -1000000

A valid minimal mutation example (mutating one hyperparameter):

```
<<<<<<< SEARCH
BIAS_STRENGTH    = 0.70
=======
BIAS_STRENGTH    = 0.65
>>>>>>> REPLACE
```

A valid 2-block mutation example (changing a hyperparameter AND a hook):

```
<<<<<<< SEARCH
INFERENCE_DTYPE               = 'fp32'   # 'fp32' | 'bf16' | 'fp16'
=======
INFERENCE_DTYPE               = 'bf16'   # 'fp32' | 'bf16' | 'fp16'
>>>>>>> REPLACE

<<<<<<< SEARCH
def patch_model(model):
    """Mutation hook: monkey-patch model attrs. Default no-op.
    Examples:
        model.rate_clip = 8.0
        model.use_checkpoint = False
    Must return model.
    """
    return model
=======
def patch_model(model):
    """Cast to bf16 + disable checkpoint for inference speedup."""
    if hasattr(model, 'use_checkpoint'):
        model.use_checkpoint = False
    return model
>>>>>>> REPLACE
```

If you find yourself writing more than ~50 lines inside `<code>`, you are doing it wrong. Stop and write a single small diff block instead.
